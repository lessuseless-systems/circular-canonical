# Circular Protocol Integration Test Configuration

# API Endpoint (required)
# Options:
#   - http://localhost:3000 (local development node)
#   - https://api.circular.org (production)
#   - https://testnet-api.circular.org (testnet)
CIRCULAR_API_URL=http://localhost:3000

# Test Credentials (loaded from ../../.env automatically)
# You can override here if needed:
# CIRCULAR_STANDARD_ADDRESS=0x...
# CIRCULAR_STANDARD_SEED=...

# Test Configuration
#NODE_ENV=test
#LOG_LEVEL=info
